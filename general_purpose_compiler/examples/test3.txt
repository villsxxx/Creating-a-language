program array_test;
var arr: array[1..5] of integer;
    i, s: integer;
begin
    i := 1;
    while i <= 5 do
    begin
        arr[i] := i * 2;
        i := i + 1;
    end;
    s := 0;
    for i := 1 to 5 do
        s := s + arr[i];
    writeln('Sum = ', s);
end.